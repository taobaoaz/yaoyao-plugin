# 2026-05-13

## yaoyao-plugin 高压测试 & Bug 修复

### 测试范围
从头到尾读了仓库所有核心文件：auto-capture, auto-recall, db-bridge, memory-store, sentiment, embedding, memory-import, memory-graph, memory-unify, memory-retain, memory-search-enhanced, session-filter, memory-tag 等。

### 发现的 Bug（13 个）

**Critical：**
1. auto-capture.ts 并发回滚竞态条件 — indexTurn 失败时 undo file append 会误删其他线程写入的内容
2. memory-import.ts 路径安全校验 startsWith 不够严谨
3. memory-import.ts 独立 DB 连接 + 事务无 ROLLBACK catch

**High：**
4. memory-search-enhanced.ts N+1 embedding 调用（性能灾难，烧 API 额度）
5. db-bridge.ts L2→cosine 公式假设向量已单位化，非 OpenAI embedding 会错
6. memory-unify.ts ESM 中直接用 require()
7. auto-capture.ts 实际确认后发现只记最后一轮是正确设计（不修改）
8. db-bridge.ts searchAll / getLatestMemory 100% 重复

**Medium：**
9. auto-recall.ts 中文 stopword "也" 重复
10. memory-graph.ts scene_blocks 中的记忆名不带 .md 后缀，与 filename map 不匹配
11. sentiment.ts emoji 遍历用 for...of 拆分 ZWJ 组合 emoji
12. auto-recall.ts buildHookResult `!appendCtx && !config` 中 `!config` 永假
13. db-bridge.ts getAllTags() 永远返回空数组（memory_meta 无 tags 列）

### 已修复（全部完成）
所有 13 个 bug 已在 /tmp/yaoyao-plugin 中修改完毕，patch 保存到：
`workspace/yaoyao-plugin-patches/2026-05-13-critical-fixes.patch`（362 行）

### 用户决策
用户说"再攒攒"——不急着发版本，patch 先存着，等更多修改一起发。

### 待办议题（用户之前提过，尚未处理）
- [ ] 心理学模型删除后，怎么让 AI 情感更丰富、更懂人
- [ ] 插件仓库拆分策略
- [ ] 用户数据迁移通用方案（需保留原有文件）

### 其他
用户续上一轮的"继续维护yaoyao插件吧"指令，高压测试执行完毕。
