
## 2026-05-15 22:15:00 — 防幻觉记忆标注（不同角度优化）

### 新增功能
- `auto-recall.ts`：召回记忆时附加**可信度元数据**，减少 AI 幻觉
  - `computeFreshness()`：时间新鲜度标签
    - 🟢 新鲜（<1天） / 🟡 近期（1-3天） / 🟠 一周前 / 🔴 N天/月前 / ⚫ 很久以前
  - `computeRelevance()`：匹配度标签
    - 🔥 高度相关（≥0.8） / ⭐ 相关（≥0.6） / ❓ 可能相关（<0.6）
  - `detectSource()`：来源标注
    - 👤 用户 / 🤖 AI / 💬 对话 / 📝 记忆
  - 记忆上下文顶部加**免责声明**："以下是从你的对话历史中召回的记忆片段。这些信息具有时效性，可能已过时。使用时请结合当前对话上下文判断其有效性，不要盲目引用旧信息。"

### 修改内容
- `src/utils/db-bridge.ts`：
  - `SearchResult` 类型新增 `createdAt` 字段
  - `search()` / `vectorSearch()` / LIKE fallback 均返回 `created_at`
- `src/hooks/auto-recall.ts`：
  - `formatRecallText()` 每条记忆格式化为 `[日期] 新鲜度 | 相关度 | 来源 表情\n内容`
  - `buildRecallContext()` 顶部加防幻觉声明

### Patch
- `/root/.openclaw/workspace/yaoyao-plugin/yaoyao-plugin-patches/2026-05-15-p5-hallucination-guard.patch`（1524 行）

### 测试结果
npm test：90 pass / 0 fail ✅

---

## 2026-05-15 — yaoyao-plugin 高压测试 Batch-3（22 项运行时/安全修复）

用户要求"改完一批再一批"，本轮为第 3 轮深度扫描，累计发现并修复 22 个 bug，全部测试通过后提交。

### 测试基线
- `npm test`：**463 pass / 0 fail** ✅
- Commit：`035ac00`

### 🔴 Critical / 安全类

| # | Bug | 文件 | 说明 |
|---|-----|------|------|
| 13 | 路径遍历 | `features/get/tool.ts` | `startsWith` 可被前缀绕过，改为 `resolve`+`relative`+`isAbsolute` |
| 14 | 路径遍历 | `features/import/tool.ts` | 同上 |
| 14b | 路径遍历 | `features/cloud-sync/tool.ts` | 同上 |
| 20 | FileDB 注入 | `platform/db/file.ts` | `delete`/`insert` 未校验 `date` 参数，传入 `../x` 可删任意文件 |
| 21 | FileDB 注入 | `utils/db-compat.ts` | 同上 |
| 8 | SSRF 重定向绕过 | `utils/llm-client.ts` | `fetch` 默认跟随 302，恶意服务端可重定向到内网 |
| 8b | SSRF 误杀公网 | `utils/llm-client.ts` | `172.` 前缀误杀 `172.32.x.x` 等公网 IP，改为精确 `172.16.0.0/12` |
| 16 | SSRF 重定向绕过 | `utils/embedding.ts` | 同上，添加 `redirect: "manual"` |

### 🟡 High / 运行时错误

| # | Bug | 文件 | 说明 |
|---|-----|------|------|
| 1 | const 重赋值 | `hooks/auto-recall.ts` | `const results` 后接 `results = results.filter(...)` |
| 2 | 接口字段缺失 | `hooks/auto-recall.ts` | `RecallThresholds` 缺 `scoreThreshold: number` |
| 3 | 空内容写入 FTS5 | `hooks/auto-capture.ts` | `indexableAsst` 为空字符串时仍调用 `db.indexTurn` |
| 4 | 裸 require ESM 报错 | `utils/healthcheck.ts` | `require("../platform/db/compat.js")` 在 ESM 下 `ReferenceError` |
| 5 | api.logger 未定义 | `features/forget/tool.ts` | `api` 不在作用域，改为 `console.error` |
| 6 | api.logger 未定义 | `features/enhanced-search/tool.ts` | 同上，改为 `console.warn` |
| 9 | `\|\|` 误杀合法 ID `0` | `utils/db-bridge.ts` | `r.id \|\| i` 导致 ID 为 `0` 时被替换，改为 `??` |
| 10 | flush 不写 index | `utils/vector/hnswlib.ts` | `flush(sync=false)` 被 `if (sync)` 包裹，定时刷新只写 meta 不写 index |
| 11 | fallback 用不存在属性 | `utils/vector/hnswlib.ts` | `JSON.parse` 失败时用 `this.dim`/`this.ef`（不存在），改为 `this.dimensions` |
| 12 | TDZ 日志引用 | `hooks/auto-recall.ts` | 三处 `api.logger.debug/info` 在 `decayed`/`deduped` 声明前引用 |
| 15 | `\|\|` 误杀 confidence `0` | `utils/l1-extractor.ts` | LLM 返回 `confidence: 0` 时被回退到 `0.7`，改为 `??` |
| 17 | JSON.parse fallback 畸形 | `entry/version.ts` | 失败时返回 `"0"`（无法解析），改为 `"0.0.0"` |
| 18 | `\|\|` 误杀 importance `0` | `hooks/auto-recall.ts` | 同上，改为 `??` |
| 19 | `\|\|` 误杀 accessCount `0` | `hooks/auto-recall.ts` | 同上，改为 `??` |
| 22 | setTimeout 泄漏 | `entry/index.ts` | `cleanerTimeout` setTimeout 引用未保存，`gateway_stop` 前无法清理 |

### 状态
- [x] Batch-3 已提交（`035ac00`）
- [x] Batch-4 已提交（`913e0af`）
- [x] Batch-5 已提交（`5a84168`）
- [ ] Batch-6 继续扫描剩余文件：optional/、mermaid-canvas.ts、secrets-loader.ts、self-improvement.ts、session-recovery.ts 等
